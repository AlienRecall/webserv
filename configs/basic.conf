server {
    server_name www.lep.com;
    host 127.0.0.1;
    port 8080;
    index index.html;

    location /upload {
        allow_methods POST;
        upload_location ./files;
    }

    location /upload-form {
        index upload.html;
    }

    location /.git {
        index 403.html;
    }
}
